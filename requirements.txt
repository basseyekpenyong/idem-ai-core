# Aziz Agent LLM backends (at least one key required)
anthropic>=0.40.0          # Option A: Claude Haiku — ANTHROPIC_API_KEY
google-genai>=1.0.0        # Option B: Gemini Flash — GOOGLE_API_KEY (free tier)
numpy>=1.26.0
scipy>=1.13.0              # Audio resampling (resample_poly)
soundfile>=0.12.1          # Audio read/write (WAV, FLAC, etc.)

# Dashboard & recording server
streamlit>=1.35.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # Required by FastAPI for file uploads

# Voice capture & STT (agent command input — English only)
sounddevice>=0.4.6
openai-whisper>=20240930   # Local Whisper model for transcription

# TTS — Microsoft Edge voices (Yoruba, Nigerian English, Igbo; no API key needed)
edge-tts>=6.1.9

# Google Drive integration
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Colab notebook generation (dataset export + Whisper fine-tuning)
nbformat>=5.10.0

# Env / config
python-dotenv>=1.0.0       # Auto-loads .env so API keys work however the server is started

# Dev & testing
pytest>=8.0.0
